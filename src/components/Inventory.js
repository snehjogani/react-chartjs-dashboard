import React, { useEffect } from 'react'
import { Container } from 'reactstrap';

const Inventory = (props) => {
  useEffect(() => { document.title = "Inventory" }, []);

  return <Container>

  </Container>
}

export default Inventory