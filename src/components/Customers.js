import React, { useEffect } from 'react'
import { Container } from 'reactstrap';

const Customers = (props) => {
  useEffect(() => { document.title = "Customers" }, []);

  return <Container>

  </Container>
}

export default Customers